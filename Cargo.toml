[package]
name = "srfs"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"

[dependencies]
actix-web = "4"
actix-files = "0"
clap = { version = "4", features = ["derive"]}
owo-colors = "1"
